// Fonction stub pour l'internationalisation future
// Actuellement retourne simplement le texte tel quel
// À développer plus tard avec un système de traduction complet

/**
 * Fonction de traduction stub
 * @param text - Le texte à traduire
 * @returns Le texte original (pas de traduction pour l'instant)
 */
export function T(text: string): string {
  return text;
}
